services:
  api:
    build:
      context: .
      dockerfile: api/Dockerfile
    network_mode: host
    env_file: .env
    environment:
      PROSPECTMAP_DB: /data/prospects.db
    volumes:
      - db_data:/data

  web:
    build:
      context: .
      dockerfile: web/Dockerfile
    network_mode: host
    environment:
      NEXT_PUBLIC_API_URL: http://localhost:8000
    depends_on:
      - api

volumes:
  db_data:
