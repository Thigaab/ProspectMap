services:
  api:
    build:
      context: .
      dockerfile: api/Dockerfile
    environment:
      PROSPECTMAP_DB: /data/prospects.db
      CORS_ORIGINS: https://prospectmap.bantou.me
    volumes:
      - db_data:/data
    expose:
      - "8000"

  web:
    build:
      context: .
      dockerfile: web/Dockerfile
      args:
        NEXT_PUBLIC_API_URL: https://api.prospectmap.bantou.me
    depends_on:
      - api
    expose:
      - "3000"

volumes:
  db_data:
